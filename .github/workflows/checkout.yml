name: "Checkout Example"

on:
  push:
    branches: [ main ]
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Show file listing and contents
        run: |
          echo "--- ls -alhR from workspace root ---"
          ls -alhR
